<%
// 设置页面变量
const pageTitle = '编辑管理员';
const pageIcon = 'fas fa-user-edit';
const pageDescription = '编辑管理员账号信息';
const breadcrumb = [
    { name: '用户管理', url: '#' },
    { name: '管理员管理', url: '/admin/admin/list' },
    { name: '编辑管理员', url: '#' }
];
%>

<div class="row">
    <div class="col-lg-8">
        <div class="card">
            <div class="card-body">
                <form action="/admin/admin/update/<%= admin.id %>" method="POST" data-ajax="true" data-validate="true">
                    <div class="mb-3">
                        <label class="form-label">用户名 <span class="text-danger">*</span></label>
                        <input type="text" class="form-control" name="username" required value="<%= admin.username %>" placeholder="登录用户名">
                        <div class="form-text">用户名也是登录账号</div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">密码</label>
                        <input type="password" class="form-control" name="password" placeholder="留空则不修改密码">
                        <div class="form-text">如果不修改密码，请留空</div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">姓名</label>
                        <input type="text" class="form-control" name="name" value="<%= admin.name %>" placeholder="真实姓名">
                    </div>

                    <div class="mb-3">
                        <label class="form-label">所属角色组 <span class="text-danger">*</span></label>
                        <select class="form-select" name="gid">
                            <% if (typeof groups !== 'undefined') { %>
                                <% groups.forEach(function(group) { %>
                                    <option value="<%= group.id %>" <%= admin.gid === group.id ? 'selected' : '' %>>
                                        <%= group.name %>
                                    </option>
                                <% }); %>
                            <% } %>
                        </select>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label class="form-label">手机号</label>
                            <input type="text" class="form-control" name="phone" value="<%= admin.phone %>" placeholder="手机号码">
                        </div>
                        <div class="col-md-6 mb-3">
                            <label class="form-label">邮箱</label>
                            <input type="email" class="form-control" name="email" value="<%= admin.email %>" placeholder="电子邮箱">
                        </div>
                    </div>

                    <div class="mb-3">
                        <div class="form-check form-switch">
                            <input class="form-check-input" type="checkbox" id="isadmin" name="isadmin" value="1" <%= admin.isadmin ? 'checked' : '' %>>
                            <label class="form-check-label" for="isadmin">超级管理员</label>
                        </div>
                        <div class="form-text text-danger">超级管理员拥有所有权限，请谨慎设置</div>
                    </div>

                    <div class="mt-4">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-save"></i> 保存修改
                        </button>
                        <a href="/admin/admin/list" class="btn btn-secondary ms-2">
                            <i class="fas fa-arrow-left"></i> 返回
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <div class="col-lg-4">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">操作提示</h5>
            </div>
            <div class="card-body">
                <ul class="text-muted small">
                    <li>修改密码时，请确保密码复杂度。</li>
                    <li>如果是自己修改资料，保存后可能需要重新登录。</li>
                    <li>超级管理员权限仅最高管理员可修改。</li>
                </ul>
            </div>
        </div>
    </div>
</div>